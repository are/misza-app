import { createMuiTheme } from '@material-ui/core/styles'

export const theme = createMuiTheme({
    palette: {
        primary: { main: '#B0BEC5', contrastText: '#000000' },
        secondary: { main: '#64FFDA', contrastText: '#000000' }
    }
})
